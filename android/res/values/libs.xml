<?xml version='1.0' encoding='utf-8'?>
<resources>
    <array name="qt_sources">
        <item>https://download.qt.io/</item>
    </array>

    <!-- The following is handled automatically by the deployment tool. It should
         not be edited manually. -->

    <array name="bundled_libs">
        <!-- %%INSERT_EXTRA_LIBS%% -->
    </array>

    <array name="qt_libs">
        <!-- Load libomp before the main library; Kirigami links OpenMP for
             palette generation and the dependency propagates to the app binary. -->
        <item>arm64-v8a;omp</item>
        <item>armeabi-v7a;omp</item>
        <item>x86_64;omp</item>
        <item>x86;omp</item>
        <!-- %%INSERT_QT_LIBS%% -->
    </array>

    <array name="load_local_libs">
        <!-- %%INSERT_LOCAL_LIBS%% -->
    </array>

</resources>
